
#include "defines.hpp"
#include <cstdint>
#include <memory>

namespace clue {
  template <uint8_t N>
  class Clusterer;
}

namespace serial {
  auto foo(float, float, float);
}

namespace cuda {
  auto foo(float, float, float);
}
